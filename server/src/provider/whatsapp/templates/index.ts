import { default as taskOverview } from './taskOverview';
import { default as taskReminder } from './taskReminder';
import { default as taskUpdate } from './taskUpdate';
import { default as taskCreate } from './taskCreate';

const WhatsappTemplates = {
	taskReminder,
	taskOverview,
	taskUpdate,
	taskCreate,
};

export default WhatsappTemplates;
