import PageLayout from '@/components/containers/page-layout';

export default async function DelegatedPage() {
	// const tasks = await TasksService.getAllTasks(organizationId);
	return (
		<PageLayout>
			<div>delegated tasks</div>
		</PageLayout>
	);
}
